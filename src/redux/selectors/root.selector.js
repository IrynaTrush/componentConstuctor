const selectType = state => state.types;
const selectOptions = state => state.currentOptions;
const selectComponents = state => state.createdComponent;

export { selectType,selectOptions,selectComponents };